[permissions]
allow = ["explore", "Edit", "Bash(cd /home/cmz488/share/tspfile/Project && python3 -c \"import py_compile; py_compile.compile('tools/tools.py', doraise=True); print('tools.py OK')\" && python3 -c \"import py_compile; py_compile.compile('main.py', doraise=True); print('main.py OK')\")", "Bash(cd /home/cmz488/share/tspfile/Project && python3 -c \"import py_compile; py_compile.compile('tools/tools.py', doraise=True); print('OK')\")", "Bash(cd /home/cmz488/share/tspfile/Project && python3 -c \"\nfrom tools.tools import RectTracker, LaserSpotDetector, detect_rect\nimport numpy as np\n\n# 1. 测试 RectTracker 基本功能\ntracker = RectTracker(track_radius=200)\nprint('RectTracker 初始化 OK:', tracker.stats)\n\n# 2. 测试 detect_rect 多线程参数\nedges = np.zeros((100, 100), dtype=np.uint8)\ngray = np.ones((100, 100), dtype=np.uint8) * 128\n# 画几个矩形\ncv2 = __import__('cv2')\nedges[20:40, 20:60] = 255\nedges[60:80, 20:60] = 255\nresult = detect_rect(edges, gray, 50, 30, 1.0, num_workers=2)\nprint('detect_rect 多线程调用 OK (result=None expected, got:', result, ')')\n\n# 3. 确认 detect_rect 单线程向后兼容\nresult2 = detect_rect(edges, gray, 50, 30, 1.0, num_workers=1)\nprint('detect_rect 单线程向后兼容 OK')\n\n# 4. 确认 reject_status 多线程聚合\nrej = {'area':0,'quad':0,'white_region':0,'aspect_ratio':0}\ndetect_rect(edges, gray, 1000, 30, 1.0, reject_status=rej, num_workers=2)\nprint('reject_status 多线程聚合:', rej)\n\nprint('全部测试通过!')\n\")", "Bash(cd /home/cmz488/share/tspfile/Project && python3 -c \"\nimport ast, sys\n\nfor f in ['tools/tools.py', 'main.py']:\n    with open(f) as fp:\n        tree = ast.parse(fp.read(), filename=f)\n    # 收集所有顶层类/函数名\n    names = set()\n    for node in ast.iter_child_nodes(tree):\n        if isinstance(node, ast.ClassDef):\n            names.add(node.name)\n        elif isinstance(node, ast.FunctionDef):\n            names.add(node.name)\n    print(f'{f}: {sorted(names)}')\nprint('AST 解析全部通过')\n\")", "Bash(cd /home/cmz488/share/tspfile/Project && python3 -c \"import py_compile; py_compile.compile('main.py', doraise=True); print('OK')\")", "review"]
